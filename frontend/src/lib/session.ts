export type User = {
  id: string;
  firstName: string;
  lastName: string;
  avatarUrl: string;
};
export type Pioneer = User;
export type Contributor = User;

// TODO: moralis login session management and user signup
